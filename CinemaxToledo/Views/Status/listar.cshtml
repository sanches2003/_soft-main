@model IEnumerable<StatusModel>
@{
    ViewData["Title"] = "listar";
}

<!--


<div class="container"> 
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h1 class="mb-0">Status</h1>
        <a href="~/status/cadastro" class="btn btn-info" style="background-color: #021532; border-color: #021532;">
            Novo
        </a>
    </div>

    <span class="alert @ViewBag.classe">@ViewBag.mensagem</span>

    <script>
        function confirmExclusao(id) {
            if (confirm("Tem certeza que deseja excluir este status")) {
                location.href = "/Status/excluir/" + id;
            }
        }
    </script>

    <table class="table table-striped">
        <thead>
            <tr>
                <th scope="col">Código</th>
                <th scope="col">Descrição</th>
                <th scope="col">Alterar</th>
                <th scope="col">Excluir</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <th scope="row">@item</th>
                    <td>@item</td>
                    <td>
                        <a href="~/status/prealterar/@item." class="btn btn-primary" style="background-color: #021532; border-color: #021532;">
                            Alterar
                        </a>
                    </td>
                    <td>
                        <a href="#" class="btn btn-primary" onclick="confirmExclusao(@item.)" style="background-color: #021532; border-color: #021532;">
                            Excluir
                        </a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
 -->
 